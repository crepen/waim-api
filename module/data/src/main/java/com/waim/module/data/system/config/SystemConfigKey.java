package com.waim.module.data.system.config;

import lombok.ToString;

public enum SystemConfigKey {
    INIT_SYSTEM,
    USER_PROTECT_ATTR_KEY,
    USER_SIGNUP_REQUIRE_ADMIN_APPROVAL,
    USER_SIGNUP_PASSWORD_REQUIREMENT,
    USER_SIGNUP_PASSWORD_REQUIRE_UPPERCASE,
    USER_SIGNUP_PASSWORD_REQUIRE_SYMBOL,
    USER_SIGNUP_PASSWORD_ALLOWED_SYMBOLS,
    USER_SIGNUP_PASSWORD_REQUIRE_NUMBER,
    USER_SIGNUP_PASSWORD_MIN_LENGTH,
    USER_SIGNUP_PASSWORD_MAX_LENGTH,
    USER_SIGNUP_ENABLED,
    SMTP_ENABLED,
    SMTP_HOST,
    SMTP_PORT,
    SMTP_USERNAME,
    SMTP_PASSWORD,
    SMTP_FROM_EMAIL,
    SMTP_FROM_NAME,
    SMTP_AUTH_ENABLED,
    SMTP_STARTTLS_ENABLED,
    SMTP_SSL_ENABLED
}
